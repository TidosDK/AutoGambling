class User {
  constructor(id, name, password, tickets, tokens) {
    this.id = id;
    this.name = name;
    this.password = password;
    this.tickets = tickets;
    this.tokens = tokens;
  }
}

export default User;
